package core.freebuild.enums;

public enum Prefix {

    GLOBALPREFIX("§a"),
    LOBBYPREFIX("§aLobby §7| §f");

    String globalPrefix;

    Prefix(String globalPrefix) {
        this.globalPrefix = globalPrefix;
    }
}
